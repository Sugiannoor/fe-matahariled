import { GeneralEntity } from "@/types/general";

export type VideoType = {
    video_id: number;
    title: string;
    embed: string;
    history_id: number; 
    history: string;
    
}&GeneralEntity